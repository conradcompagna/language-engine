/** Mutable feature state; initialized explicitly by the ordered bootstrap. */
export const tokenMapState = {
  RESOLUTION_DISPLAY_INFO_BY_CATEGORY: undefined,
  _pendingPanelSurfaceLookupPromises: undefined
};
